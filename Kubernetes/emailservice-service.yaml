apiVersion: v1
kind: Service
metadata:
  name: emailservice-service
spec:
  type: ClusterIP # Expone el servicio solo internamente en el clúster.
  selector:
    app: emailservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80