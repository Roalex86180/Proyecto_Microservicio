name: CI/CD - Authentication Service

on:
  push:
    # branches:
    #   - main
    # paths:
    #   - "microservices/authenticationservice/**"
    #   - "kubernetes/**"
    #   - ".github/workflows/authentication-service.yml"

env:
  AZURE_RESOURCE_GROUP: ${{ secrets.AZURE_RESOURCE_GROUP }}
  AZURE_ACR_NAME: ${{ secrets.AZURE_ACR_NAME }}
  AKS_CLUSTER_NAME: ${{ secrets.AKS_CLUSTER_NAME }}
  SERVICE_NAME: "authenticationservice"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Inspect microservice folder deeply
        run: |
          ls -la ./microservices/${{ env.SERVICE_NAME }}
          cat ./microservices/${{ env.SERVICE_NAME }}/Dockerfile || echo "Dockerfile no encontrado"

      - name: Azure login
        uses: azure/login@v1
        with:
          creds: '{"clientId":"${{ secrets.AZURE_CLIENT_ID }}", "clientSecret":"${{ secrets.AZURE_CLIENT_SECRET }}", "subscriptionId":"${{ secrets.AZURE_SUBSCRIPTION_ID }}", "tenantId":"${{ secrets.AZURE_TENANT_ID }}"}'

      - name: Build and tag Docker image
        run: |
          docker build \
            -f ${{ github.workspace }}/microservices/${{ env.SERVICE_NAME }}/Dockerfile \
            -t ${{ env.AZURE_ACR_NAME }}.azurecr.io/${{ env.SERVICE_NAME }}:${{ github.sha }} \
            ${{ github.workspace }}

      - name: Docker login
        run: docker login ${{ env.AZURE_ACR_NAME }}.azurecr.io -u ${{ secrets.AZURE_CLIENT_ID }} -p ${{ secrets.AZURE_CLIENT_SECRET }}

      - name: Push Docker image to ACR
        run: docker push ${{ env.AZURE_ACR_NAME }}.azurecr.io/${{ env.SERVICE_NAME }}:${{ github.sha }}

      - name: Set AKS credentials
        uses: azure/aks-set-context@v1
        with:
          creds: '{"clientId":"${{ secrets.AZURE_CLIENT_ID }}", "clientSecret":"${{ secrets.AZURE_CLIENT_SECRET }}", "subscriptionId":"${{ secrets.AZURE_SUBSCRIPTION_ID }}", "tenantId":"${{ secrets.AZURE_TENANT_ID }}"}'
          resource-group: ${{ env.AZURE_RESOURCE_GROUP }}
          cluster-name: ${{ env.AKS_CLUSTER_NAME }}

      - name: Deploy to AKS
        run: |
          kubectl apply -f ./kubernetes/authenticationservice-deployment.yaml
          kubectl apply -f ./kubernetes/authenticationservice-service.yaml
